"""astrolabe — celestial data collection & analysis over public surveys.

Local library + CLI: fetch from surveys (Gaia, SDSS, ...) into a Parquet catalog,
query it with DuckDB, and run pure analysis functions over astropy Tables.
"""

__version__ = "0.1.0"
